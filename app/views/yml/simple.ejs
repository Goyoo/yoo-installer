#cloud-config
hostname: <%= hostname%>
ssh_authorized_keys:
<% sshAuthorizedKeys.forEach(function(key){%>  - <%= key%>
<%})%>
hostname: coreos
coreos:
  units:
    - name: etcd.service
      command: start
    - name: fleet.service
      command: start